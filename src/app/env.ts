export const env = {
  apprite: {
    endPoint: String(process.env.PUBLIC_APPRITE_HOST_URL),
    projectId: String(process.env.PUBLIC_APPRITE_PROJECT_ID),
    apiKey: String(process.env.APPRITE_API_KEY),
  },
};
